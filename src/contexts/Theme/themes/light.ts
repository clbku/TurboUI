import { theme } from 'antd';

export const light = {
    algorithm: theme.defaultAlgorithm as any,
    token: {
    },
    components: {
        Typography: {
            titleMarginBottom: '0',
            titleMarginTop: '0',
        },
    },
};
